import Navigation from '../navigation/Navigation';
import { StyledHeader } from './Header.styles';

const Header = () => {
  return (
    <StyledHeader>
      <div>here will be logo</div>

      <Navigation />
    </StyledHeader>
  );
};

export default Header;
